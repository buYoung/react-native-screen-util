declare global {
    interface Number {
        marginTop(): number;

        marginBottom(): number;

        marginLeft(): number;

        marginRight(): number;

        mt(): number;

        mb(): number;

        ml(): number;

        mr(): number;

        paddingTop(): number;

        paddingBottom(): number;

        paddingLeft(): number;

        paddingRight(): number;

        pt(): number;

        pb(): number;

        pl(): number;

        pr(): number;
    }
}
export {}